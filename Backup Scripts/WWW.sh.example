#!/bin/bash

DestFolder="/home/pi/backups/vms/www"
MySQLPW=""

mysqldump --all-databases --single-transaction --quick --lock-tables=false > /tmp/sql-dump.sql -u root -p$MySQLPW # Dump Entire DB into /tmp/.
rsync -e 'ssh -p 1010' -az /tmp/sql-dump.sql pi@chse.dev:$DestFolder/WWW-SQL-Dump.sql # Transfer to VM.
rm /tmp/sql-dump.sql # Remove Locally.

rsync -e 'ssh -p 1010' -azrd --delete /var/www/* pi@chse.dev:$DestFolder/var-www/

rsync -e 'ssh -p 1010' -az /etc/apache2/sites-available/www.conf pi@chse.dev:$DestFolder/www.conf # Transfer to VM.
rsync -e 'ssh -p 1010' -az /etc/apache2/apache2.conf pi@chse.dev:$DestFolder/apache2.conf # Transfer to VM.
rsync -e 'ssh -p 1010' -az /etc/apache2/conf-enabled/block-ips.conf pi@chse.dev:$DestFolder/block-ips.conf # Transfer to VM.
rsync -e 'ssh -p 1010' -az /etc/letsencrypt/options-ssl-apache.conf pi@chse.dev:$DestFolder/options-ssl-apache.conf # Transfer to VM.
rsync -e 'ssh -p 1010' -azrd --delete /etc/modsecurity/* pi@chse.dev:$DestFolder/modsecurity/ # Transfer to VM.
